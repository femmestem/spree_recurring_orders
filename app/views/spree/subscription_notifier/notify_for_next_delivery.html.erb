<p>Hi <%= @subscription.parent_order.email %></p>
<div>
  <p>
    <%= Spree.t('subscription_notifier.notify_for_next_delivery.order_details') %>
  </p>
  <table>
    <% variant = @subscription.variant %>
    <tr>
      <td><%= variant.sku %></td>
      <td>
        <%= raw( variant.product.name) %>
        <%= raw( variant.options_text) %>
      </td>
      <td>(<%= @subscription.quantity %>) <%= Spree.t('at_symbol') %> </td>
    </tr>
    
    <tr>
      <td></td>
      <td>
        <%= Spree.t(:item_price) %>
      </td>
      <td>
        <%= @subscription.price %>
      </td>
    </tr>
  </table>
</div>

<p>
  Your next order is going to be deliver <%= @subscription.prior_notification_days_gap %> days later.
  Your <%= @subscription.frequency.title.capitalize %> subscription has <%= @subscription.number_of_deliveries_left %> deliveries left
</p>
<p>Regards</p>
